var names = "Deven";

for (let i = 0; i < names.length; i++) {
    console.log(names[i].toUpperCase());
}

namesArray = ["Deven", "Pavithra", "Dulli", "Duggu"];

for (let index = 0; index < namesArray.length; index++) {
    console.log(namesArray[index].toUpperCase())
    
}